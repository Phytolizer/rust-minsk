{
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "preLaunchTask": "rust: cargo build",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/mc",
            "sourceLanguages": [
                "rust"
            ]
        }
    ]
}
